# SEO / Performance / Accessibility — Audit (2026-04-17)

**Site:** [turnberryplaceforsale.com](https://www.turnberryplaceforsale.com)  
**Stack:** Next.js 14 (Pages Router), Vercel  
**Note:** Scores below are **Lighthouse 13.x against production** at audit time (lab data; not CrUX field data). Re-run after deploying this branch.

## Lighthouse — category scores

Scores are 0–100 (Lighthouse multiplies internally as 0–1 in JSON).

### Desktop (`--preset=desktop`)

| URL | Performance | Accessibility | Best Practices | SEO |
|-----|-------------|-----------------|----------------|-----|
| `/` | 85 | 92 | 77 | 100 |
| `/available-condos` | 88 | 90 | 77 | 100 |
| `/towers` | 97 | 96 | 77 | 100 |
| `/amenities` | 94 | 100 | 77 | 100 |
| `/stirling-club` | 87 | 96 | 77 | 100 |
| `/agent` | 98 | 92 | 77 | 100 |

### Mobile (`--form-factor=mobile`, homepage only)

| URL | Performance | Accessibility | Best Practices | SEO |
|-----|-------------|-----------------|----------------|-----|
| `/` | 36 | 88 | 77 | 100 |

### Core Web Vitals (lab, homepage desktop snapshot)

| Metric | Value | Flag (threshold) |
|--------|-------|------------------|
| LCP | ~1.9 s | Good (&lt; 2.5 s) |
| FCP | ~0.6 s | Good |

Mobile homepage LCP/TBT drive the low Performance score; treat **mobile LCP & main-thread work** as the top perf priority.

---

## ✅ Fixed in this PR (`chore/seo-audit`)

- **Structured data / AI citation:** FAQ copy updated to the **eight** prompt questions (aligned with `TURNBERRY_FAQ_ITEMS` + visible `HomeFaqSection`). `RealEstateAgent` includes `recognizedBy` (Nevada Real Estate Division) and `identifier` license string; `knowsAbout` / `areaServed` expanded per spec.
- **ApartmentComplex:** Default description and amenity list aligned with spec; **`containsPlace`** lists four tower `Residence` stubs with `numberOfRooms` range.
- **Geo consistency:** `36.1408` / `-115.1564` in `lib/schema/geo.ts`, homepage meta (`geo.position` / `ICBM`), map center, interactive map default, `map-place-schema`, and neighborhood embed (**query-based** Google Maps URL for 2827 Paradise Rd).
- **Sitemap:** `/available-condos?tower=1` … `tower=4` entries added in `pages/sitemap.xml.ts` (this repo uses Pages Router, not `app/sitemap.ts`).
- **robots.txt:** Explicit `Allow: /` for Googlebot, Bingbot, GPTBot, CCBot, PerplexityBot, ClaudeBot, Google-Extended, Applebot-Extended (in addition to `User-agent: *`).
- **Accessibility statement:** “Last updated” uses **build-time** `toLocaleDateString` (no stale “January 2026”).
- **Neighborhood / hyperlocal:** Prime location copy mentions **Fashion Show Mall** and **Las Vegas Monorail** alongside existing Wynn/Encore/Convention Center context.

---

## 🟡 Quick wins — next sprint

- **Mobile Performance (homepage ~36):** Prioritize LCP image (hero `fetchPriority`, responsive `sizes`, consider AVIF/WebP delivery audit on CDN), reduce JS on critical path, defer non-critical scripts.
- **Best Practices (~77 sitewide):** Review Lighthouse “third-party cookies”, “deprecations”, and console errors on production (often analytics/widgets).
- **Re-run Lighthouse** on staging/production after each release; add `lighthouse-*.json` to `.gitignore` (done) for local runs.

---

## 🔴 Needs product / stakeholder decision

- **Mobile Performance ≥ 90:** May require hero asset swap, font subsetting, or reducing above-the-fold widgets; confirm budget with design.
- **ItemList / Product JSON-LD for `/available-condos`:** Deferred (MLS/IDX compliance and RealScout attribution — do not scrape listings into schema without broker approval).
- **`sameAs` social URLs for Dr. Jan:** Omit until verified (per policy).

---

## Top 3 highest-leverage changes (ranked)

1. **Mobile LCP & blocking time** — Largest gap vs stated goals; improves real users and AI crawler render quality.
2. **Image pipeline** — Hero + gallery: `alt` audit, `priority` only where needed, lazy below fold (ongoing; partially addressed in prior work).
3. **Post-deploy validation** — Schema.org validator + Rich Results Test on `/`, `/towers` after JSON-LD/geo edits; Google Search Console for sitemap coverage.

---

## Follow-ups

- [ ] Merge `feat/live-inventory` → `main`, then `feat/jsonld-schema`, then `chore/seo-audit` (or squash per your Git workflow).
- [ ] Re-run Lighthouse after deploy; update this file if scores move materially.
